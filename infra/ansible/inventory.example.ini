[mongo]
# Replace these placeholders with the real hosts once access is available.
# Keep the names stable because the replica set config uses them.
mongo1 ansible_host=SERVER_IP ansible_user=SSH_USER mongo_replica_priority=2
mongo2 ansible_host=PI_IP ansible_user=SSH_USER mongo_replica_priority=1
mongo3 ansible_host=MAC_IP ansible_user=SSH_USER mongo_replica_priority=1

[mongo_primary]
mongo1

